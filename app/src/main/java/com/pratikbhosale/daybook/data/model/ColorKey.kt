package com.pratikbhosale.daybook.data.model

/**
 * Curated set of color keys for bucket accents. Never stored as arbitrary strings —
 * the set is finite and controlled per DESIGN.md.
 */
enum class ColorKey {
    TODAY,
    WEEK,
    SOMEDAY,
    CUSTOM1,
    CUSTOM2,
    CUSTOM3,
}
